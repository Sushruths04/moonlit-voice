# DreamVoice pinned dependencies.
# VoxCPM2 card/GitHub: pip install voxcpm; Python >=3.10, PyTorch >=2.5.0, CUDA >=12.0.
# MiniCPM5-1B card: Transformers path uses transformers >=5.6 plus accelerate and torch.
gradio==6.18.0
torch==2.11.0
torchaudio==2.11.0
voxcpm==2.0.3
transformers==5.12.0
accelerate==1.14.0
huggingface_hub==1.19.0
soundfile==0.14.0
librosa==0.11.0
numpy==2.2.6
modal==1.5.0

# --- Stretch (install only when enabling that feature) ---
# cohere            # S4 Tiny Aya / Transcribe
# (FLUX.2-klein deps per Black Forest Labs card)        # S2 cover art
